#include <string.h>
#include <stdint.h>
#include <stdlib.h>
#include <stdbool.h>

int compare(int a, int b) {
	    return b - a;		//all that needs to change is the order that we compare the elements
}